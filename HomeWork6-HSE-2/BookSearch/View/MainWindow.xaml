<Controls:MetroWindow x:Class="BookSearch.MainWindow"
                      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                      xmlns:Controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
                      ResizeMode="CanResizeWithGrip"
                      WindowTransitionsEnabled="False"
                      WindowStartupLocation="CenterScreen"
                      DataContext="{Binding Main, Source={StaticResource Locator}}"
                      Title="Google books shelf" Height="350" Width="525" MinHeight="300" MinWidth="400">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="3"/>
            <ColumnDefinition Width="*"  MinWidth="150" MaxWidth="350"/>
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="45"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid Grid.Column="0" Grid.Row="0" Margin="5">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="5*"/>
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>
                <TextBox Grid.Column="0" Margin="5" TextAlignment="Center" />
                
                <Button Grid.Column="1" />
            </Grid>
            <ListView Grid.Column="0" Grid.Row="1" Margin="5" BorderBrush="White">
                <ListView.View>
                    <GridView>
                        <GridViewColumn DisplayMemberBinding="{Binding Title}" Header="Title" />
                        <GridViewColumn DisplayMemberBinding="{Binding Author}" Header="Author" />
                        <GridViewColumn DisplayMemberBinding="{Binding Language}" Header="Language" />
                        <GridViewColumn DisplayMemberBinding="{Binding PublishDate}" Header="Year" />
                    </GridView>
                </ListView.View>
            </ListView>
        </Grid>
        <GridSplitter Grid.Column = "1" HorizontalAlignment="Stretch" />
    </Grid>
</Controls:MetroWindow>
